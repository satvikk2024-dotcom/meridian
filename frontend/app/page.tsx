export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-bg-primary">
      <span
        className="font-mono text-accent tracking-[0.3em] uppercase"
        style={{ fontSize: "32px", letterSpacing: "0.3em" }}
      >
        Meridian
      </span>
    </main>
  );
}
